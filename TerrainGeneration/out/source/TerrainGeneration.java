/* autogenerated by Processing revision 1297 on 2025-03-20 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import peasy.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class TerrainGeneration extends PApplet {



PeasyCam cam;
int cols, rows;
int scl = 10;
float[][] terrain;
float flying = 0;
boolean paused = false;

public void setup() {
    /* size commented out by preprocessor */;
    textSize(32);
    textAlign(RIGHT, TOP);
    
    cols = width  / scl;
    rows = height / scl;
    cam = new PeasyCam(this, 1500);
    terrain = new float[cols+1][rows+1];
}

public void draw() {
    if(!paused) {
        background(0);
        translate(-cols*scl*0.5f, -rows*scl*0.5f, 0);
        rotateX(PI/4);

        setupTerrain();
        showTerrain();
        showFrameRate();
    }
}

public void keyPressed() {
    if(key == ' ') {
        paused = !paused;
    }
}

public void setupTerrain() {
    flying -= 0.1f;
    float yOff = flying;
    
    for(int y = 0; y < rows + 1; y++) {
        float xOff = 0;
        for(int x = 0; x < cols + 1; x++) {
            terrain[x][y] = map(noise(xOff, yOff), 0, 1, -50, 50);
            xOff += 0.2f;
        }
        yOff += 0.2f;
    }
}

public void showTerrain() {
    stroke(255, 100);
    noFill();

    for(int y = 0; y < rows; y++) {
        beginShape(TRIANGLE_STRIP);
        for(int x = 0; x < cols; x++) {
            vertex(x * scl, y * scl, terrain[x][y]);
            vertex(x * scl, (y+1) * scl, terrain[x][y+1]);
        }
        endShape();
    }
}

public void showFrameRate() {
    pushMatrix();
    pushStyle();
    cam.beginHUD();

    fill(0, 255, 0);
    text("FPS: " + PApplet.parseInt(frameRate), width - 20, 20);

    cam.endHUD();
    popStyle();
    popMatrix();
}


  public void settings() { fullScreen(P3D); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "TerrainGeneration" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
